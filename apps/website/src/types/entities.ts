export type Artist = {
  id: string;
  name: string;
  image: string;
  link: string;
  popularity: number;
};

export type Article = {
  slug: string;
  title: string;
  image: string;
  author: string;
  date: string;
};
